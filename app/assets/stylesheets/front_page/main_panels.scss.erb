.main_header {
  height: 20px;
  border-bottom: groove 7px;
}

.main_footer {
  height: 60px;
}

.main_body {
  column-width: 270px;
    min-height: 100%;
  display: table;
}

.main_title {
  float: left;
  margin: 0;
  font-family: "Lucida Grande", Iverdana, arial, helvetica, sans-serif;
  position: absolute;
  top: 50%;
  transform: translate(0, -50%);
}

.main_nav_atop_panel {
  font-family: "Lucida Grande", Iverdana, arial, helvetica, sans-serif;
  font-size: 14px;
  text-decoration: none;
  position: absolute;
  right: 20px;
  top: 50%;
  transform: translate(0, -50%);
}