<%= form_for(@problem_list) do |f| %>
    <% if @problem_list.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(@problem_list.errors.count, "error") %> prohibited this problem_list from being
                saved:</h2>

            <ul>
                <% @problem_list.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <div class="field">
        <%= f.label :title %><br>
        <%= f.text_field :title, :autofocus => true %>
    </div>
    <div class="field">
        <%= f.label :description %><br>
        <%= f.text_area :description %>
    </div>
    <div class="actions">
        <%= f.submit %>
    </div>
<% end %>
